# 交互与视觉规范

## 常驻浮标

- 默认尺寸：48–64px 方形或圆角方形；
- 有清晰图标和悬停反馈，不显示长文字；
- 轻微阴影、半透明背景、足够对比度；
- 支持拖动和“锁定位置”；
- 启动时若旧位置不可见，自动回到当前活动显示器的安全角落；
- 托盘菜单提供“显示浮标 / 打开设置 / 退出”，作为浮标丢失时的恢复入口。

## 点击后的动作菜单

只显示核心动作：

1. 截图并钉住；
2. 截图翻译；
3. 打开剪贴板；
4. 设置。

菜单可以短暂获取焦点；点击空白处或执行动作后关闭。浮标闲置时不应成为当前输入目标。

## 截图选区

- 只针对触发瞬间鼠标所在的显示器；
- 明确显示选区边界和尺寸；
- `Esc` 取消；
- 选区完成后立即显示“已复制 / 已钉住 / 翻译中”等真实状态；
- 没有屏幕录制权限时显示原因和系统设置入口，不显示空白结果。

## 图片浮窗

- 默认置顶但不遮挡过大区域；
- 支持拖动、缩放、透明度调整；
- 复制、关闭按钮始终可找到；
- 图片浮窗关闭不删除历史，删除历史也不应误关其他窗口。

## 历史窗口

- 顶部为“文字 / 图片 / 固定”筛选；
- 每项显示时间、类型和简短预览；
- 复制是主操作，删除和清空放在明确的次级位置；
- 清空必须有一次可理解的确认，并提供取消；
- 搜索只查本地记录。

## 设置窗口

- 使用普通可聚焦窗口；
- 快捷键冲突显示“注册失败及原因”，不能静默回退；
- 权限、采集开关和翻译配置分组；
- 所有设置重启后仍可验证。

## 焦点原则

- 浮标：不承担文本输入，默认不抢焦点；
- 动作菜单：只在用户点击后暂时获取焦点；
- 设置/历史：正常获取焦点；
- 第一版不做自动粘贴，避免把内容粘到浮标或错误窗口。
