# 截图剪贴板浮块

## 一句话目标

做一个面向个人的 macOS / Windows 桌面工具：平时只显示一个小悬浮浮标，点击或按快捷键即可截图、钉住图片、复制/翻译内容，或查找过去的文字与图片剪贴板。

## 使用者与主要问题

使用者是经常在浏览器、文档和聊天工具之间切换的个人用户。主要问题不是缺少一个“大而全”的工作台，而是：

1. 截图后想暂时把图片放在屏幕上对照；
2. 想快速复制截图或翻译截图中的文字；
3. 想找回刚才复制过的文字或图片；
4. 不希望常驻面板遮挡内容、抢焦点或增加视觉负担。

## 设计原则

- 小入口、按需展开；
- 本地优先、默认不上传；
- 复制与钉住是独立动作；
- 任何失败都要给出可理解的原因和恢复动作；
- 单屏场景先做到稳定，再考虑扩展；
- 功能完整度以最终验收矩阵为准，不以中间切片命名产品版本。

## 成品必须交付的能力

### 悬浮入口

- 约 48–64px 的无边框悬浮浮标；
- 可拖动、锁定、隐藏和恢复；
- 始终置顶但不在闲置状态抢键盘焦点；
- 点击后出现紧凑动作菜单。

### 截图与钉住

- 快捷键或菜单进入触发瞬间鼠标所在显示器的截图选区；
- 选区后可复制为图片；
- 可生成独立置顶图片窗口；
- 图片窗口支持移动、缩放、透明度、复制和关闭。

### 截图翻译

- 截图后可选择识别并翻译；
- 翻译服务通过适配器接入，不硬编码密钥；
- 未配置或网络失败时保留原图，并明确显示失败原因；
- 不因翻译失败阻塞截图、复制和钉住。

### 剪贴板历史

- 文字与图片分栏；
- 搜索、复制、固定、删除、清空；
- 数量和单项大小上限可配置；
- 历史默认本地保存，可一键关闭采集。

主动截图与系统剪贴板监听分开：关闭“采集剪贴板”只停止被动监听，用户主动截取的图片仍可保存到图片历史。

### 设置

- 截图、打开浮标、打开历史、翻译等快捷键；
- 浮标位置、透明度、启动时运行、历史保留规则；
- 权限检查与错误提示；
- 翻译服务配置入口（不在仓库保存密钥）。

## 明确不在本次范围

- 多显示器联动、跨显示器选区和跨电脑同步；若机器连接多个屏幕，只处理鼠标所在屏幕；
- 云端账号、云盘冲突解决、团队协作；
- 录音、笔记、链接收藏、凭据管理、待办；
- 自动把内容粘贴回原应用；
- App Store 上架、公开云端服务和远程控制。

## 可观察的成品定义

在一台 macOS 笔记本和一台 Windows 笔记本上，用户都能：启动应用 → 看到浮标 → 用快捷键完成截图 → 复制或钉住 → 在历史窗口找回文字/图片 → 修改快捷键 → 重启后设置仍有效。权限缺失、快捷键冲突和翻译服务不可用时，界面必须给出明确状态，不能假装成功。
