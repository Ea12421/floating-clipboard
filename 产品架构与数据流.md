# 产品架构与数据流

## 四层产品骨架

| 层 | 内容 | 作用 |
|---|---|---|
| 数据层 | 截图文件、剪贴板条目、固定状态、快捷键设置、权限状态、翻译结果缓存 | 保存本地可复用的事实 |
| 能力层 | 单屏截图、剪贴板监听、去重、OCR/翻译适配器、置顶窗口、全局快捷键 | 把系统输入加工成可用结果 |
| 场景层 | 快速截图、截图钉住、截图翻译、历史查找、复制回用、设置管理 | 组合能力解决实际任务 |
| 触达层 | 悬浮浮标、动作菜单、图片浮窗、历史窗口、设置窗口、托盘入口 | 让用户快速触发和查看 |

## 关键数据流

### 沉淀流

```text
系统截图 / 系统剪贴板
        ↓
去重、类型识别、大小限制、敏感内容过滤
        ↓
本地记录库 + 图片文件目录
```

### 回补流

```text
本地历史记录
        ↓ 用户搜索/选择
复制到系统剪贴板或重新打开图片浮窗
        ↓
用户继续在原应用中工作
```

### 翻译流

```text
截图
  ↓
本地临时图像
  ↓ 用户主动点击翻译
OCR/翻译适配器
  ↓
结果展示 + 可复制；失败时保留原图并显示原因
```

## 本地数据对象

### ClipboardItem

- `id`：本地唯一 ID；
- `kind`：`text` 或 `image`；
- `content`：文字内容，或图片文件相对路径；
- `fingerprint`：用于去重的哈希；
- `createdAt` / `lastUsedAt`；
- `pinned`；
- `source`：系统剪贴板或截图；
- `sizeBytes`；
- 删除时物理移除记录和图片文件，不保留 `deletedAt` 软删除字段。

### AppSettings

- 浮标位置与锁定状态；
- 快捷键映射；
- 是否采集剪贴板；
- 历史数量/大小上限；
- 启动与主题设置；
- 翻译 provider 的非秘密配置引用。

秘密（API Key、Cookie、密码）不得进入普通设置文件、Git 或日志。

## 状态边界

- 浮标状态：`hidden → idle → menu-open`；
- 截图状态：`idle → selecting → captured → copied → pinned/closed`；
- 翻译状态：`not-requested → running → success/failed`；
- 剪贴板采集：`disabled/enabled`；
- 权限状态：`unknown/granted/denied`。

截图目标显示器的唯一规则是：用户触发截图时鼠标所在的显示器。状态变化必须由用户动作或系统事件驱动，不能由模型自行扩大权限或自动打开云同步。
