# 截图剪贴板浮块项目协作规则

## 项目定位

这是一个面向个人使用的 macOS / Windows 桌面工具：以一个小型悬浮浮标作为常驻入口，快速完成截图、钉住图片、复制、截图翻译和剪贴板历史管理。

## 当前冻结边界

- 以单台笔记本为主要场景；每次截图只处理触发瞬间鼠标所在的显示器，不做跨显示器工作区管理和跨屏选区。
- 本地优先；当前不做账号、云端同步、云盘同步或远程控制。
- 必须有：悬浮浮标、截图、截图后复制、图片钉住、文字/图片剪贴板历史、快捷键、设置窗口。
- 截图翻译作为可插拔能力；未配置翻译服务时，基础截图和剪贴板功能仍可独立使用，并明确提示未配置原因。
- 当前不做：录音转写、笔记、链接收藏、凭据管理、待办、团队协作、自动发布。
- 默认不自动把剪贴板内容上传或自动粘贴到原应用；复制动作只写入系统剪贴板，自动粘贴必须另行设计和显式触发。
- 用户主动触发的截图可以写入图片历史；“采集剪贴板”开关只控制系统剪贴板监听，不应阻止主动截图保存。
- 删除/清空采用物理删除：同时删除本地记录与对应图片文件，不使用未定义的软删除。

## 事实源与交付规则

- 本目录内的 `PROJECT.md`、`产品架构与数据流.md`、`交互与视觉规范.md`、`技术设计.md`、`实施计划与闭环验收.md`、`执行蓝图.md`、`DESIGN.md` 是设计事实源。
- `RUN_STATE.json` 是跨回合实现状态；`STATE.md` 只保存人类可读摘要，不复制完整历史。
- 实现可以按“闭环切片”推进，但切片只是内部施工顺序，不是对用户宣称的半成品版本。只有最终验收矩阵全部通过，才可称为成品可用。
- 每个切片必须留下可运行结果、验证证据和准确的未完成项；不得用构建成功或模型自评代替真实使用验收。
- 任何新增依赖、账号/密钥、网络服务、数据库迁移或打包发布都要在对应文档和状态中记录，不把密钥写入仓库。
- 借鉴 MIT 项目代码时保留许可证与版权声明，并先做依赖和素材审计。

## 行为与代码边界

- 浮标空闲时尽量不抢焦点；菜单、设置和历史窗口可以正常获取焦点。
- 位置以当前活动显示器的可见工作区为准，启动时校正；不把旧坐标当作永远有效。
- 截图必须处理逻辑像素与物理像素换算，不能只在单一缩放比例下通过。
- 主进程负责系统能力和持久化，renderer 只通过白名单 preload API 访问；不打开任意远程页面。
- 剪贴板历史必须可关闭、有数量/大小上限、可删除和清空，并过滤密码管理器的隐藏剪贴板类型。
