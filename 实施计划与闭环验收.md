# 实施计划与闭环验收

## 施工原则

下面的“切片”只是开发顺序，不是对外版本，也不代表做完一片就可以宣布产品完成。每片都必须形成可运行闭环；最终只有整张验收矩阵通过，才交付成品。

## 闭环切片

### 切片 1：应用骨架与浮标

交付：应用可启动、托盘可见、浮标可显示/隐藏/拖动/锁定，设置窗口可打开。

验证：macOS 与 Windows 各启动一次；重启后位置和锁定状态可回读；浮标闲置不抢输入焦点。

### 切片 2：本地存储与剪贴板历史

交付：文字/图片采集开关、去重、历史窗口、复制/固定/删除/清空、上限清理。

验证：复制重复内容只保留一条；关闭系统剪贴板采集后不新增被动条目但主动截图仍可保存；重启后历史可回读；清空后数据库记录和图片文件均物理删除。

### 切片 3：单屏截图与图片钉住

交付：快捷键/菜单截图、选区取消、复制图片、独立图片浮窗。

验证：至少在 macOS Retina 和 Windows 常见缩放下各完成一次真实截图；截图目标固定为鼠标所在显示器；截图边界与选区一致；权限缺失时有明确提示。

### 切片 4：快捷键与设置完善

交付：快捷键配置、冲突反馈、启动设置、透明度/位置设置、权限状态页。

验证：修改后立即生效；占用快捷键时显示失败原因；重启后设置保持；托盘可恢复浮标。

### 切片 5：截图翻译适配器

交付：翻译 provider 接口、用户配置入口、翻译中/成功/失败状态和结果复制。

验证：有 provider 时返回真实结果；无 provider、超时、限流时不影响原图和复制；密钥不出现在仓库或日志。

### 切片 6：打包与成品验收

交付：macOS、Windows 安装包，安装/卸载说明和最终验收记录。

验证：两平台全流程通过，关键权限和快捷键状态可观察；无同步、无账号、无未声明功能；用户实际操作后确认可用。

## 最终验收矩阵

| 能力 | 通过条件 |
|---|---|
| 浮标 | 启动、隐藏、恢复、拖动、锁定均可用 |
| 截图 | 快捷键和菜单均可触发，选区正确，可取消 |
| 钉住 | 图片可置顶、移动、缩放、透明度调整、关闭 |
| 复制 | 文字和图片都能写入系统剪贴板 |
| 历史 | 分栏、搜索、复制、固定、删除、清空有效 |
| 设置 | 快捷键、位置、启动和采集开关可修改并持久化 |
| 翻译 | 成功与失败均有真实状态，失败不破坏基础能力 |
| 安全 | 无密钥入库，无默认云端上传，路径和 IPC 有边界 |
| 跨平台 | macOS 与 Windows 各有安装包和真实运行证据 |

## 停止条件

- 核心截图或历史数据会丢失；
- 权限失败没有可理解的恢复路径；
- 需要引入账号/同步才能完成当前范围；
- 发现实现正在扩大到多显示器、云同步或无关模块；
- 同一错误经过一次定位、最小修正和专项验证仍未解决。

## 回滚方式

- 每个切片独立提交并记录验证证据；
- 进入切片 1 前先建立仅包含冻结文档的 Git 基线提交；
- 出现回归时回退到上一个通过验收的切片，不删除用户本地数据；
- 数据结构变更必须有迁移前备份和反向迁移说明；
- 任何未通过的切片保持明确状态，不把它标成成品。
