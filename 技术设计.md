# 技术设计

## 推荐技术路线

- 桌面壳：Electron；
- 语言：TypeScript；
- 界面：React + Vite（浮标、菜单、历史、设置共用组件）；
- 本地持久化：SQLite，图片单独存放在应用工作区；
- 进程边界：Electron main 负责系统能力，renderer 负责界面，preload 只暴露白名单 IPC；
- 打包：macOS 与 Windows 分别构建安装包；不依赖 App Store。

选择 Electron 是为了复用已有桌面工具的经验，并同时覆盖 macOS 与 Windows；跨平台不等于所有系统行为完全相同，系统适配层必须独立。

## 默认实现决策

为避免实现阶段反复重开设计，先冻结以下默认值；只有真实验证证明不合适才调整：

- 本地数据库使用 `better-sqlite3`，放在 Electron `app.getPath('userData')` 下；启动时执行编号迁移，迁移前保留一份数据库备份。
- 图片放在同一 `userData` 下的固定 `images/` 目录，使用临时文件写入后原子重命名。
- 截图使用 Electron `desktopCapturer` 加透明选区层；平台差异收口在 adapter，不把 macOS 专属脚本带入共享层。
- 剪贴板默认最多保留 200 条、单张图片不超过 10MB、总本地历史不超过 256MB；达到上限时按最旧且未固定优先清理。
- 剪贴板图片统一转为 PNG 保存；主动截图始终可以进入图片历史，系统剪贴板监听开关只控制被动监听。
- 全局快捷键统一返回 `registered / conflict / invalid` 三种结果；冲突不静默回退。
- 翻译默认目标语言为简体中文，超时 30 秒；provider 通过 OpenAI-compatible 接口适配，未配置时功能明确标记为不可用。
- provider 密钥只通过 Electron `safeStorage` 写入系统安全存储，绝不写入普通设置、日志或 Git。
- 打包使用 `electron-builder`：macOS DMG/ZIP，Windows NSIS/便携包；不做商店发布。

## 模块边界

```text
main/
├─ window-manager        浮标、菜单、图片浮窗、设置/历史窗口
├─ screenshot-adapter    当前活动显示器截图与选区
├─ clipboard-adapter     文字/图片读取、监听、写回
├─ shortcut-adapter       全局快捷键注册与冲突反馈
├─ storage               SQLite、图片文件、原子写入、清理
├─ translation-adapter    OCR/翻译 provider 接口
└─ permission-adapter    屏幕录制等系统权限检查
renderer/
├─ bubble
├─ action-menu
├─ history
├─ settings
└─ pinned-image
preload/
└─ typed allowlist IPC
```

## 单屏与缩放策略

- 以触发截图时鼠标所在显示器为唯一目标，不做跨屏选区；
- 位置保存为“相对当前可见工作区”的安全位置，并在启动时校正；
- 截图选区统一经过逻辑像素 → 物理像素转换；
- macOS Retina 与 Windows 100%/125%/150% 缩放都要有至少一个真实检查样本；
- 若系统返回的显示器信息不可用，停止截图并报告原因，不生成伪成功文件。

## 剪贴板策略

- 采集默认关闭，用户主动开启；
- 文本和图片分别处理，采用内容指纹去重；
- 有数量、单项大小和总空间上限（默认值见上）；
- 过滤密码管理器的隐藏类型；
- 所有删除/清空均可观察和验证；
- 第一版只做写入系统剪贴板，不自动向原窗口发送粘贴键。

## 翻译策略

定义统一接口：

```ts
translateImage(input: { path: string; sourceLanguage?: string; targetLanguage: string })
  -> { text: string; provider: string; durationMs: number }
```

provider 可以是本地 OCR/翻译或用户配置的 OpenAI-compatible 服务。密钥通过系统安全存储或运行时注入，不进入仓库。没有 provider 时，界面显示“未配置翻译服务”，而不是伪造翻译结果。

## 安全边界

- `contextIsolation: true`、`nodeIntegration: false`、renderer sandbox；
- 不允许 renderer 任意读写路径；
- 图片路径必须限制在应用工作区，拒绝路径穿越和符号链接；
- 外部链接只在用户明确动作下交给系统浏览器；
- 日志不记录剪贴板全文、图片内容、密钥或 Cookie；
- 当前不包含网络同步，因此本地数据不会自动离开设备。

## 失败处理

每项系统能力都要返回结构化结果：`success / reason / recoverable`。权限缺失、快捷键被占用、图片写入失败和翻译超时必须在 UI 中显示具体原因，并给出重试、设置或继续使用基础功能的路径。
