# 执行蓝图

> 版本：Draft 0.1｜状态：视觉方向已选定，等待进入实现｜适用范围：单台笔记本、鼠标所在显示器、本地优先

## 1. 这件事要交付什么

这是一个低打扰的 macOS / Windows 桌面工具：平时只保留一个小浮标，用户在当前工作流中需要时，通过点击或快捷键完成截图、复制、钉住、翻译，或找回文字/图片剪贴板历史。

产品的核心不是“再做一个面板”，而是把三个高频动作压缩为一条可靠路径：

```text
需要暂存 / 带走内容
        ↓
截图或复制
        ↓
即时反馈（已复制 / 已钉住 / 翻译中 / 失败原因）
        ↓
继续原应用工作，必要时从本地历史回补
```

## 2. 冻结范围与完成定义

### 必须完成

- 小型置顶浮标：显示、隐藏、恢复、拖动、锁定，不在闲置时抢焦点；
- 当前触发瞬间鼠标所在显示器的截图选区、取消、复制；
- 图片钉住窗口：置顶、移动、缩放、透明度、复制、关闭；
- 文字与图片剪贴板历史：开关、去重、搜索、复制、固定、删除、清空、上限清理；
- 快捷键、位置、启动与历史规则设置，并在重启后保持；
- 可插拔翻译：未配置、成功、超时、限流和网络失败均有真实状态，不破坏原图与复制；
- macOS 与 Windows 安装包及两平台真实运行证据。

### 明确不做

账号、云同步、云盘、远程控制、多显示器联动、跨屏选区、录音、笔记、链接、凭据、待办、自动粘贴、公开云服务和商店发布。

### 成品完成定义

在两台目标笔记本上，用户可以完成：启动 → 看见浮标 → 快捷键截图 → 复制或钉住 → 从历史找回文字/图片 → 修改快捷键 → 重启后设置仍有效。权限缺失、快捷键冲突和翻译不可用时，界面必须显示原因和恢复路径。

## 3. 事实源与决策顺序

1. `AGENTS.md`：项目边界和安全规则；
2. `PROJECT.md`：产品目标与用户场景；
3. `产品架构与数据流.md`：对象、数据流和状态边界；
4. `交互与视觉规范.md`：行为和视觉硬约束；
5. `技术设计.md`：默认实现路线与平台适配；
6. `实施计划与闭环验收.md`：切片验收和最终矩阵；
7. 本文：把上述事实编排成可执行的工程主线；
8. `DESIGN.md`：界面实现级视觉底稿，不得反向扩大产品范围。

冲突处理顺序：安全 / 隐私边界 → 产品范围 → 用户可完成的主要任务 → 平台事实 → 视觉偏好。

## 4. 系统分层与责任

```text
main process
  ├─ window-manager       浮标、菜单、历史、设置、图片窗口
  ├─ screenshot-adapter   鼠标所在显示器、选区、DPI 转换
  ├─ clipboard-adapter    读取、监听、写回、隐藏类型过滤
  ├─ shortcut-adapter     注册、冲突、失效原因
  ├─ storage              SQLite、图片文件、迁移、清理
  ├─ translation-adapter  OCR / provider、超时与错误映射
  └─ permission-adapter   屏幕录制等平台权限
renderer
  ├─ bubble / action-menu / history / settings / pinned-image
preload
  └─ typed allowlist IPC
```

硬边界：renderer 不读任意路径、不持有密钥、不直接调用系统能力；所有能力返回结构化结果（`success / reason / recoverable`）。

## 5. 默认技术决策（本轮先按此实现）

| 主题 | 默认决策 | 调整触发条件 |
|---|---|---|
| 桌面壳 | Electron + TypeScript + React/Vite | 真实平台能力无法满足，才评估原生替代 |
| 数据库 | `better-sqlite3`，位于 Electron `userData`，编号迁移，迁移前备份 | 性能或原生模块构建在目标平台失败 |
| 图片 | `userData/images/`，临时文件后原子重命名，统一 PNG | 实测发现体积或色彩保真不可接受 |
| 截图目标 | 触发瞬间鼠标所在显示器 | 平台 API 无法稳定返回显示器信息 |
| 历史开关 | 只控制被动系统剪贴板监听；主动截图可写入图片历史 | 隐私测试显示用户无法理解该区别 |
| 删除 | 物理删除数据库记录与图片文件，不使用未定义软删除 | 需要可恢复回收站时另立设计 |
| 上限 | 200 条、单图 10MB、总历史 256MB；最旧且未固定优先清理 | 真实使用样本证明默认值不合适 |
| 快捷键 | `registered / conflict / invalid`；冲突不静默回退 | 平台 API 能提供更具体原因时扩展错误码 |
| 翻译 | 默认目标语言简体中文，30 秒超时，OpenAI-compatible adapter | 用户确认本地 provider 或多语言需求 |
| 密钥 | Electron `safeStorage`，普通设置只存 provider 引用 | 目标系统安全存储不可用 |
| 打包 | `electron-builder`：macOS DMG/ZIP，Windows NSIS/便携包 | 安装、签名或原生模块兼容性失败 |

## 6. 闭环施工顺序

### Slice 0：基线与工程入口

建立仅包含冻结文档的 Git 基线，初始化 Electron 工程目录、脚本、类型检查和状态记录；不接入任何外部服务。

**通过证据**：可重复安装依赖、`typecheck` 通过、main/preload/renderer 入口可定位、基线 commit 可回滚。

### Slice 1：应用骨架与浮标（第一施工单元）

交付启动、托盘、浮标显示/隐藏/恢复、拖动、锁定、设置窗口入口；位置按当前可见工作区校正并持久化。

**通过证据**：macOS 与 Windows 各启动一次；重启回读位置与锁定；浮标闲置不抢键盘焦点；托盘可恢复浮标。

### Slice 2：本地存储与剪贴板历史

接入 SQLite、图片目录、文本/图片类型识别、指纹去重、采集开关、搜索、复制、固定、物理删除、清空和上限清理。

**通过证据**：重复内容只保留一条；关闭被动采集后不新增被动条目；主动截图仍可入图片历史；重启可回读；清空后记录和图片均无残留。

### Slice 3：单屏截图与图片钉住

实现鼠标所在显示器识别、选区 overlay、Esc 取消、逻辑/物理像素转换、图片复制和独立置顶窗口。

**通过证据**：macOS Retina、Windows 100%/125%/150% 至少各一个真实样本；边界与选区一致；权限拒绝有原因和系统设置入口。

### Slice 4：快捷键与设置完善

实现快捷键配置与冲突反馈、启动运行、透明度、位置、权限状态页和托盘恢复路径。

**通过证据**：修改立即生效；冲突显示失败原因；重启保持；设置窗口可键盘操作。

### Slice 5：截图翻译适配器

实现 provider 接口、非秘密配置引用、密钥安全存储、翻译中/成功/失败/超时/限流状态和结果复制。

**通过证据**：有 provider 返回真实结果；无 provider 或失败不影响原图、复制、钉住；日志与仓库无密钥。

### Slice 6：打包与成品验收

构建 macOS/Windows 安装包，完成安装、卸载、升级/覆盖安装和最终验收矩阵。

**通过证据**：两平台全流程真实运行；权限、快捷键、错误、恢复、无云同步/账号等边界可观察；用户实际操作确认可用。

## 7. 每个切片的统一交付包

每片结束必须留下：

- 可运行结果（不是只通过构建）；
- 变更文件与依赖清单；
- 真实验证命令、平台、分辨率/缩放、输入和结果；
- 失败、未完成项和下一步；
- 如涉及数据结构，迁移前备份与反向迁移说明；
- 对应状态更新，但不把中间切片称为成品。

## 8. 视觉与交互执行原则

视觉主线见 `DESIGN.md`，执行时必须保持：

- 浮标是入口，不是常驻面板；
- 颜色、文字、图标、位置至少两种通道表达关键状态；
- action state、data state、safety/permission state 分开，不用一个颜色语义覆盖全部；
- 复制是历史窗口的主操作，删除/清空是次级且有确认；
- 动效只解释打开、选择、完成、失败和恢复，不以“更多动画”制造完成感；
- 所有关键任务都有键盘、静态、低动效和错误恢复路径。

## 9. 风险与停止条件

立即停在当前切片并记录 `blocked` 的情况：

- 截图或历史数据有丢失风险；
- 权限失败没有可理解的恢复动作；
- renderer 越过 preload 边界或日志泄露内容/密钥；
- 需要账号、同步、多显示器或无关模块才能继续；
- 同一错误经过一次定位、最小修正和专项验证仍未解决；
- 只能以 mock、模型自评或构建成功替代真实运行证据。

## 10. 当前唯一下一步

视觉方向已确认，后续不再生成第二套风格。确认执行后：

1. 建立文档 Git 基线；
2. 启动 Slice 1；
3. 先交付可启动、可恢复、可拖动、可锁定且不抢焦点的浮标闭环。

浅色/深色只作为同一 `DESIGN.md` 的主题状态继续实现；不新增独立视觉方向。

## 11. 参考材料及借鉴边界

本蓝图参考了 `/Users/m4air/视觉设计系统` 中的：

- `STYLE_PROFILES_V0.2.md` 的“产品科技”任务编排：问题/主张 → 输入 → 可观察状态 → 结果/限制 → follow-up；
- `DESIGN_TOKENS_V0.2.md` 的语义 token 与 action/data/safety 状态分离；
- `MEDIUM_RULES_V0.2.md` 的对象、动作、状态、scope、证据语义守恒；
- `LOW_QUALITY_PATTERNS_V0.2.md` 的失败恢复、通道冗余、伪状态和装饰噪音审计；
- `WEB_DESIGN_SKILL_DISTILLATION.md` 的参考输入分层、反模式优先、真实载体验收和差异审计。

这些材料用于抽象方法，不代表本项目已经完成同题实验，也不复制任何品牌、模板、字体组合或第三方资产。
